{
  "name": "@charlie/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n worker,spa -c blue,magenta \"wrangler dev\" \"vite\"",
    "dev:worker": "wrangler dev",
    "dev:spa": "vite",
    "build": "vite build",
    "preview": "wrangler dev",
    "deploy": "vite build && wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run",
    "typecheck": "vue-tsc --noEmit -p tsconfig.app.json && tsc --noEmit -p tsconfig.worker.json",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply charlie-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply charlie-db --remote"
  },
  "dependencies": {
    "hono": "^4.12.30",
    "jose": "^6.2.3",
    "vue": "^3.5.40",
    "vue-router": "^4.6.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260716.1",
    "@vitejs/plugin-vue": "^6.0.8",
    "concurrently": "^9.1.2",
    "vite": "^8.1.5",
    "vue-tsc": "^2.2.0",
    "wrangler": "^4.111.0"
  }
}
